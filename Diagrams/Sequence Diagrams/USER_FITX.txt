title Sequence Diagram FitX

participant USER
participant COMPUTER
participant HOME
participant WORKOUT_PROGRAM
participant PROGRAM
participant PASSWORD
participant BLOG
participant DIET
participant SHOP
participant CART
participant SERVER


USER->COMPUTER:Connect...
COMPUTER->SERVER:Requesting Connection
COMPUTER<--SERVER:
COMPUTER->HOME:Landing Page (Connection Successful)

HOME->WORKOUT_PROGRAM: Connect to Workout Program Page
WORKOUT_PROGRAM->SERVER:Requesting Connection
COMPUTER<--SERVER:
USER->SERVER:login(user, pswd)
USER<--SERVER:
USER->SERVER:register(user, pswd)
USER<--SERVER:
COMPUTER->WORKOUT_PROGRAM:Workout Program Page (Connection Successful)

WORKOUT_PROGRAM->PROGRAM:add_program()
PROGRAM->SERVER:add_information()
WORKOUT_PROGRAM<--SERVER:

WORKOUT_PROGRAM->PASSWORD: i.password()
PASSWORD->SERVER: change_password()
WORKOUT_PROGRAM<--SERVER:
HOME<-WORKOUT_PROGRAM:home()

HOME->BLOG: Connect to Blog Page
BLOG->SERVER:Requesting Connection
COMPUTER<--SERVER:
COMPUTER->BLOG:Connection Successful
HOME<-BLOG: home()

HOME->DIET: Connect to Diet Page
DIET->SERVER:Requesting Connection
COMPUTER<--SERVER:
COMPUTER->DIET:Connection Successful
HOME<-DIET: home()

HOME->SHOP: Connect to Shop Page
SHOP->SERVER:Requesting Connection
COMPUTER<--SERVER:
COMPUTER->SHOP:Connection Successful
SHOP->CART: add_cart()
CART->SERVER:Requesting Procces
SHOP<--SERVER:
SHOP->CART: checkout_cart()
CART->SERVER:Requesting Procces
SHOP<--SERVER:
HOME<-SHOP: home()

USER->HOME:logout()
USER<--HOME: